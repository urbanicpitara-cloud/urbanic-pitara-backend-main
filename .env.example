# ============================================================
# URBANIC PITARA - BACKEND ENVIRONMENT VARIABLES
# ============================================================
#
# DEVELOPMENT: Copy this file to .env and fill in development values
# PRODUCTION: Set these as environment variables in your deployment platform
#
# Platforms:
#   - Render.com: Dashboard → Environment
#   - Vercel/Fly.io: Dashboard → Settings → Environment Variables  
#   - AWS/Azure: Use Secrets Manager
#
# ============================================================

# ============================================================
# 🔧 CORE APPLICATION SETTINGS
# ============================================================

NODE_ENV=development
PORT=4000

# ============================================================
# 🗄️  DATABASE (PostgreSQL)
# ============================================================
#
# Development:
#   postgresql://postgres:password@localhost:5432/urbanidb
#
# Production (Supabase):
#   postgresql://postgres:password@db.xxxx.supabase.co:5432/postgres?sslmode=require&channel_binding=require
#
# Production (Neon):
#   postgresql://neondb_owner:password@ep-xxx.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
#

DATABASE_URL=postgresql://postgres:BraiinyPs%40super01pass@localhost:5432/urbanicpitaradb

# ============================================================
# 🔐 AUTHENTICATION & SECURITY
# ============================================================
#
# JWT_SECRET:
#   ⚠️  IMPORTANT: Must be at least 32 characters long!
#   Generate: openssl rand -base64 32
#   OR: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# JWT_EXPIRY:
#   How long JWT tokens remain valid
#   Examples: "7d", "24h", "1h"
#

JWT_SECRET=your-super-secure-jwt-secret-key-minimum-32-chars-long
JWT_EXPIRY=7d

# ============================================================
# 🌍 CORS & FRONTEND URLs
# ============================================================
#
# Development:
#   CORS_ORIGIN=http://localhost:3000
#   FRONTEND_URL=http://localhost:3000
#   BACKEND_URL=http://localhost:4000
#
# Production:
#   CORS_ORIGIN=https://urbanic-pitara.com
#   FRONTEND_URL=https://urbanic-pitara.com
#   BACKEND_URL=https://api.urbanic-pitara.com
#

CORS_ORIGIN=http://localhost:3000
FRONTEND_URL=http://localhost:3000
BACKEND_URL=http://localhost:4000

# ============================================================
# 📧 EMAIL (SMTP) - Optional but Recommended
# ============================================================
#
# For password resets and order notifications
#
# Option 1 - Gmail (Development):
#   1. Enable 2FA: https://myaccount.google.com/security
#   2. Generate App Password: https://myaccount.google.com/apppasswords
#   3. Use email@gmail.com and app-password below
#
# Option 2 - SendGrid (Production Recommended):
#   1. Signup: https://sendgrid.com
#   2. Create API key
#   3. Use apikey@sendgrid.net and API key below
#
# Option 3 - AWS SES:
#   1. Setup: https://docs.aws.amazon.com/ses/
#   2. Generate SMTP credentials
#

SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password-here
SMTP_SECURE=true
FROM_EMAIL=noreply@urbanic-pitara.com

# ============================================================
# 💳 PHONEPE PAYMENT GATEWAY
# ============================================================
#
# Development (Mock Mode - No Real Charges):
#   PHONEPE_MOCK=true
#   Use test credentials below
#   No real PhonePe account needed
#
# Production (Real Mode - Processes Real Payments):
#   PHONEPE_MOCK=false
#   Get production credentials from PhonePe dashboard
#   Must have approved merchant account
#
# Getting Real Credentials:
#   1. Contact PhonePe: sales@phonepe.com
#   2. Complete merchant onboarding (1-3 days)
#   3. Get MERCHANT_ID and SALT_KEY
#   4. Generate OAuth credentials (CLIENT_ID, CLIENT_SECRET)
#

# ---- Test Credentials (Mock Mode) ----
PHONEPE_MERCHANT_ID=PGTESTPAYUAT
PHONEPE_SALT_KEY=099eb0cd-02cf-4e2a-8aca-3e6c6aff0399
PHONEPE_SALT_INDEX=1
PHONEPE_ENV=UAT
PHONEPE_API_URL=https://api-preprod.phonepe.com/apis/pg-sandbox
PHONEPE_TOKEN_URL=https://api-preprod.phonepe.com/apis/pg-sandbox
PHONEPE_CLIENT_VERSION=v1

# ---- OAuth Credentials (Test - Optional) ----
# PHONEPE_CLIENT_ID=your-client-id
# PHONEPE_CLIENT_SECRET=your-client-secret

# ---- Mode Selection ----
PHONEPE_MOCK=true

# ============================================================
# 🔍 ERROR LOGGING & MONITORING (Optional)
# ============================================================
#
# For production error tracking, consider free alternatives:
#   - LogRocket: https://logrocket.com (free tier available)
#   - Datadog: https://www.datadoghq.com (free community plan)
#   - Sentry: https://sentry.io (has paid plans, no free tier)
#
# Current setup: Simple error logging to console and database
#

# MONITORING_SERVICE= # Optional for future integrations

# ============================================================
# ⚙️  OPTIONAL SETTINGS
# ============================================================

TRUST_PROXY=false

# ============================================================
# 📝 NOTES
# ============================================================
#
# Development Workflow:
#   1. Copy this file to .env
#   2. Fill in dev values (localhost URLs, test credentials)
#   3. npm run dev
#   4. Works with both test and real PhonePe keys
#
# Production Deployment:
#   1. Set DATABASE_URL to production PostgreSQL
#   2. Change CORS_ORIGIN to production domain
#   3. Set PHONEPE_MOCK=false
#   4. Add real PhonePe credentials
#   5. Generate new JWT_SECRET
#   6. Setup SMTP with production email service
#   7. Add SENTRY_DSN for error tracking
#   8. Deploy to Render/Vercel/Railway
#
# Security Best Practices:
#   ✅ NEVER commit .env to Git
#   ✅ Use environment variables (not .env) in production
#   ✅ Rotate JWT_SECRET every 6 months
#   ✅ Use different keys for dev/staging/production
#   ✅ Enable MFA on email account for password resets
#   ✅ Use strong database passwords (30+ characters)
#
